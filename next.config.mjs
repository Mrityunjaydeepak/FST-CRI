/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'export',
    // trailingSlash: true, // Enables static export
    // images: {
    //   unoptimized: true, // Necessary if using `next/image` with static export
    // },

};

export default nextConfig;